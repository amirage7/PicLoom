# AI Image Canvas 桌面版验证记录

验证日期：2026-08-25
ChatGPT 适配器：`2026-08-25.1`
Electron：`37.10.3`
平台：Windows 11 x64

## 自动验证

- 前端：86 项测试通过，TypeScript 与 Vite 生产构建通过。
- 桌面端：90 项测试通过，TypeScript 类型检查与构建通过。
- 后端：48 项测试通过。
- 扩展兼容模式：10 项测试通过，生产构建通过。
- PyInstaller 后端 EXE：健康检查、项目读取、临时 SQLite 创建通过。
- Electron `win-unpacked`：应用启动成功，内置后端仅监听 `127.0.0.1:8001`。
- NSIS：`AI Image Canvas-Setup-0.2.0.exe` 已生成。
- 生命周期：正常关闭窗口后 `127.0.0.1:8001` 立即释放，无 PyInstaller 后端残留。

## 需要用户本人完成的账号验证

状态：待手动确认。

1. 打开右侧“ChatGPT”标签并点击“登录 / 查看 ChatGPT”。
2. 在内嵌的官方页面中手动登录普通 ChatGPT 网页账号。
3. 准备两张图片，在“图片详情”中分别改名为“假面骑士build”和“喜羊羊”；确认同项目重复名称会显示冲突且不会覆盖原图。
4. 输入 `将@假面骑士build的身体和@喜羊羊的头部合成一个新的角色`，确认每次输入 `@` 都出现带缩略图的候选并可选择。
5. 点击生成，确认两张参考图都上传到 ChatGPT，完成后新图自动导入 Canvas，且版本连线来自第一张引用图“假面骑士build”。
6. 点击新图“查看原图”，确认首次完整显示整张图；放大到 125%，关闭后再次打开应恢复“适应”。
7. 点击“保存原图”，确认可选择本地路径并得到完整文件。
8. 退出并重新打开应用，确认 ChatGPT 会话、图片名称和 Canvas 项目仍然存在。

本记录不保存账号标识、Cookie、聊天记录或包含私人内容的截图。
